sudo apt update
sudo apt install software-properties-common
sudo add-apt-repository --yes --update ppa:ansible/ansible
sudo apt install ansible

Error/Warning: If the user is not in the sudoers list, run:
su
sudo usermod -aG sudo "username"
su "username"

ansible --version

Error: If UTF-8 encoding is expected, run:
sudo nano /etc/default/locale
Update encoding to: LANG="en_US.UTF-8", LC_CTYPE="en_US.UTF-8"
(Or) Run in terminal: sudo update-locale LANG=en_US.UTF-8 LC_CTYPE=en_US.UTF-8
Restart the system.

ssh-keygen -t rsa -C "devops default"
ls -la .ssh
cat .ssh/id_rsa.pub
cat .ssh/id_rsa

ssh-copy-id -i ~/.ssh/id_rsa.pub "ipaddress"

ssh -i ~/.ssh/"keyname" "ipaddress"

Error: If "port 22, Connection Refused" occurs, run:
sudo apt install openssh-server
sudo systemctl restart sshd

ssh "Ip address"

sudo nano inventory

[servers]
192.168.1.219
192.168.1.218

ansible all --key-file ~/.ssh/id_rsa -i inventory -m ping

ansible all -i inventory -m ping

sudo nano playbook.yaml

---
- name: Echo
  hosts: servers
  connection: local
  tasks:
  - name: print ping message
    debug:
      msg: Hello!

ansible-playbook -i inventory playbook.yml















